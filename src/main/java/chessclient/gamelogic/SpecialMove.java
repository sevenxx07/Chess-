package chessclient.gamelogic;

/**
 * Enum SpecialMove defines types of special behavior a Move can have.
 * Currently special moves are castle, en passant, and promotion.
 */
public enum SpecialMove {
    EN_PASSANT,
    CASTLE,
    PROMOTION;
}
